<div class="jumbotron">
  <div class="container">
    <div class="col-md-8">
      <h3>Welcome, <%= "#{@user.first_name} To Your #{@user.role} Dashboard" %></h3>
      <% if @user.role == "merchant" %>
        <p><a class="btn btn-primary btn-lg" href="/dashboard/users/<%= @user.id %>/items" role="button">Your Items</a>
      <% end %>
    </div>
  </div>
